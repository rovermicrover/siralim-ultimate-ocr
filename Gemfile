source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby "3.1.2"

# Use ruby wrapper for imagemagick
gem 'rmagick'
# Use ruby wrapper for tesseract for image OCR, requires tesseract to be installed
gem 'rtesseract'